<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TuristenokApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Статус-бар прозрачный -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Полноэкранный режим -->
        <item name="android:windowFullscreen">true</item> <!-- Скрытие статус-бара -->
    </style>

    <style name="Theme.TuristenokApp" parent="Base.Theme.TuristenokApp" />
    
    <style name="FullScreenDialogTheme" parent="Theme.AppCompat.DayNight.DialogWhenLarge">

    </style>
</resources>
